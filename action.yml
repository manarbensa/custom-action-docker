name: "Docker Message Action"
description: "An action to send messages to a Docker container."
inputs:
  message:
    description: "The message to send to the Docker container."
    required: true
outputs:
  response:
    description: "The response from the Docker container."
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.message }}